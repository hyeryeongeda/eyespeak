# 아이트래킹 파이프라인 (3단계)
# 설치: 가상환경 사용 권장 (시스템 Python externally managed 오류 방지)
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# 자세한 내용: docs/SETUP.md
opencv-python>=4.8.0
numpy>=1.24.0
flask>=3.0.0

# L2CS-Net gaze 모델
torch>=2.0.0
torchvision>=0.15.0

# MediaPipe: 얼굴 검출·Face Mesh (헤드포즈 폴백)
mediapipe>=0.10.0
