# 개발 서버(Dev)용 override
# 환경: Dev | 프론트 https://dev.j14e205.p.ssafy.io | 백엔드 API https://dev.j14e205.p.ssafy.io/api/v1
# 사용: docker-compose -f docker-compose.yml -f docker-compose.dev.yml --profile blue up -d
services:
  front-blue:
    ports:
      - "8010:80"
    build:
      context: .
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL:-https://dev.j14e205.p.ssafy.io/api/v1}
