# Docker Compose 로컬 (docker-compose.local.yml)
# 복사해 .env.local 로 저장 후 값 채움
# 실행: docker-compose --env-file .env.local -f docker-compose.yml -f docker-compose.local.yml up -d

MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=eyespeak
