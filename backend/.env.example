# 로컬 개발용 DB 환경변수 (application-local 프로필)
# 이 파일을 복사해서 .env 로 저장한 뒤 값을 채운 뒤,
# IDE Run Configuration 의 Environment variables 에서 로드하거나
# 터미널에서 export DB_USERNAME=root DB_PASSWORD=비밀번호 후 실행

DB_USERNAME=root
DB_PASSWORD=
